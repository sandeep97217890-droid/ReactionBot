# Telegram API credentials from https://my.telegram.org
APP_ID=12345678
APP_HASH=your_app_hash_here

# (Optional) Pre-exported string session – skip interactive login on first run
# SESSION_STRING=

# (Optional) Path to the session file (default: session.session)
# SESSION_FILE=session.session

# (Optional) Path to the SQLite database file (default: reactions.db)
# DB_PATH=reactions.db
